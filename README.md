#### Install dependencies

```bash
  pip install -r requirements.txt
```


#### Replace Credentials
-  `Create a .env file in the root directory and replace the values with your own:`
```bash
  CLIENT_ID=your_client_id
  CLIENT_SECRET=your_client_secret
```
---

#### Run: `app.py`
